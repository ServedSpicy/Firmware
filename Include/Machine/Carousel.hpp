
#include <Arduino.h>


namespace Carousel {

    /**
     * @brief Rorates the carousel until the
     * specified container is at the front.
     *
     * @param container The container id.
     */

    void moveTo(byte container);


    void home();
}
